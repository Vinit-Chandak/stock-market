name: pnl_tracker
description: "Stock P&L Tracker - Track your trading profits and losses"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  excel: ^4.0.6
  file_picker: ^8.1.6
  intl: ^0.19.0
  provider: ^6.1.2
  fl_chart: ^0.70.2
  google_fonts: ^6.2.1
  syncfusion_flutter_pdf: ^27.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
